import { HotelPipePipe } from './hotel-pipe.pipe';

describe('HotelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HotelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
