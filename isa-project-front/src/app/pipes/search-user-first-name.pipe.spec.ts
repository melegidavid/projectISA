import { SearchUserFirstNamePipe } from './search-user-first-name.pipe';

describe('SearchUserFirstNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUserFirstNamePipe();
    expect(pipe).toBeTruthy();
  });
});
