import { FromLocationPipe } from './from-location.pipe';

describe('FromLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new FromLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
